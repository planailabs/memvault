[package]
name = "memvault-doc"
version = "0.1.0"
edition = "2024"

[dependencies]
memvault-core = { path = "../memvault-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_ipld_dagcbor = "0.6"
blake3 = "1"
thiserror = "2"

[dev-dependencies]
