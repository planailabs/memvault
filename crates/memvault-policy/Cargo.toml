[package]
name = "memvault-policy"
version = "0.1.0"
edition = "2024"

[dependencies]
memvault-core = { path = "../memvault-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
regex = "1"
aho-corasick = "1"
blake3 = "1"
thiserror = "2"
tracing = "0.1"
rand = "0.8"

[dev-dependencies]
