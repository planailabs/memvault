[package]
name = "memvault-extract"
version = "0.1.0"
edition = "2024"

[dependencies]
memvault-core = { path = "../memvault-core" }
memvault-policy = { path = "../memvault-policy" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tracing = "0.1"
pulldown-cmark = "0.12"
scraper = "0.22"
pdf-extract = "0.7"
docx-rs = "0.4"

[dev-dependencies]
