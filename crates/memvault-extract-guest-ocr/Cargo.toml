[package]
name = "memvault-extract-guest-ocr"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
memvault-extract-abi = { path = "../memvault-extract-abi" }
extism-pdk = "1"
